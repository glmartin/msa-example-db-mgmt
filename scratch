mkdir sqitch
cd sqitch/
docker pull sqitch/sqitch:0.9998
docker build -t local/sqitch:1.2 ,
mkdir migrations
chmod 777 migrations/
docker run -ti --entrypoint /bin/bash --privileged=true -v ${PWD}/migrations:/migrations:z local/sqitch:1.2
(docker container)> sqitch init restapi --uri https://github.com/glmartin/restapi_db --engine pg



Assumed the following has been run:
    sqitch init restapi --uri https://github.com/glmartin/restapi_db --engine pg
    sqitch target add restapi db:pg://myuser@db:5432/restapi
    sqitch engine add pg restapi
    sqitch config --bool deploy.verify true
    sqitch config --bool rebase.verify true
